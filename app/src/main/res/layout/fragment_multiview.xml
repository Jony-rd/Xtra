<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/multiviewRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:clipToPadding="false"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/multiviewContent"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <FrameLayout
            android:id="@+id/videoGrid"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black"
            android:clipToPadding="false" />

        <!-- Used only for the special two-stream landscape + chat-off layout.
             The second stream occupies the top half; functional chat is placed
             into the bottom half at runtime. -->
        <FrameLayout
            android:id="@+id/specialRightColumn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_gravity="end"
            android:background="@android:color/transparent"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/controlsOverlay"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_margin="8dp"
            android:background="@drawable/bg_stream_overlay"
            android:gravity="center_vertical"
            android:minHeight="48dp"
            android:orientation="horizontal"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:visibility="visible">

            <ImageButton
                android:id="@+id/backButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/multiview_close"
                android:padding="12dp"
                android:src="@drawable/baseline_arrow_back_black_24"
                app:tint="@android:color/white" />

            <TextView
                android:id="@+id/multiviewTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/multiview"
                android:textAppearance="?attr/textAppearanceTitleSmall"
                android:textColor="@android:color/white" />

            <TextView
                android:id="@+id/activeAudio"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="@android:color/white"
                tools:text="Audio: channel" />

            <ImageButton
                android:id="@+id/pipButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/multiview_minimize"
                android:padding="12dp"
                android:src="@drawable/ic_picture_in_picture"
                android:visibility="gone"
                app:tint="@android:color/white" />

            <ImageButton
                android:id="@+id/chatButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/multiview_chat"
                android:padding="12dp"
                android:src="@drawable/ic_settings_chat"
                app:tint="@android:color/white" />

            <ImageButton
                android:id="@+id/seekToLiveButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Seek to live"
                android:padding="12dp"
                android:src="@drawable/ic_seek_to_live"
                app:tint="@android:color/white" />

            <ImageButton
                android:id="@+id/addStreamButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/multiview_add_stream"
                android:padding="12dp"
                android:src="@drawable/ic_add"
                app:tint="@android:color/white" />

            <ImageButton
                android:id="@+id/layoutButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/multiview_layout"
                android:padding="12dp"
                android:src="@drawable/ic_multiview"
                app:tint="@android:color/white" />

            <ImageButton
                android:id="@+id/moreButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/multiview_more_options"
                android:padding="12dp"
                android:src="@drawable/baseline_more_vert_black_24"
                app:tint="@android:color/white" />
        </LinearLayout>
    </FrameLayout>

    <LinearLayout
        android:id="@+id/chatContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.42"
        android:background="?android:colorBackground"
        android:orientation="vertical"
        android:visibility="gone">

        <LinearLayout
            android:id="@+id/chatHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorSurfaceContainer"
            android:gravity="center_vertical"
            android:minHeight="52dp"
            android:orientation="horizontal"
            android:paddingStart="12dp"
            android:paddingTop="4dp"
            android:paddingEnd="8dp"
            android:paddingBottom="4dp">

            <TextView
                android:id="@+id/chatTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/multiview_chat"
                android:textAppearance="?attr/textAppearanceTitleSmall" />

            <ImageButton
                android:id="@+id/combinedChatButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/multiview_all_chats"
                android:padding="12dp"
                android:src="@drawable/ic_multiview"
                app:tint="?attr/colorControlNormal" />
        </LinearLayout>

        <FrameLayout
            android:id="@+id/chatContent"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="?android:colorBackground" />
    </LinearLayout>
</LinearLayout>
